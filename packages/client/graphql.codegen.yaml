schema: http://localhost:3002/graphql
generates:
  ./src/main/gen/api-types.ts:
    plugins:
      - typescript
    config:
      nonOptionalTypename: true
      maybeValue: T | null | undefined
      declarationKind: 'interface'
      strictScalars: true
      scalars:
        Date: string
        DateTime: string
